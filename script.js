notDoneToDoArea = document.querySelector("#not-done-todo")
doneToDoArea = document.querySelector("#done-todo")

class ToDo {
    constructor(text) {
        this.text = text
    }

    generateElements = () => {
        return 
    }
}